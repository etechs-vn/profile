meta {
  name: Get Feed
  type: http
  seq: 2
}

get {
  url: {{base_url}}/tenants/{{tenant_id}}/social/user/:user_id/feed
}

params:path {
  user_id: {{user_id}}
}

params:query {
  skip: 0
  limit: 20
}
