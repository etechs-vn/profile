meta {
  name: Send Request
  type: http
  seq: 1
}

post {
  url: {{base_url}}/tenants/{{tenant_id}}/profiles/user/:user_id/connections
}

params:path {
  user_id: {{user_id}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "receiver_id": 2
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
